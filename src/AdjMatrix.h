/*
 * A graph can also be represented as an adjacency matrix. This representation 
 * is very useful many times.
 */

class AdjMatrix{

	/* Number of nodes in a graph. Alternatively number of rows in a
	 * matrix. */
	int N; 

	/* Number of edges in a graph. Alternatively number of columns in a
	 * matrix. */
	int M; 

	public:
	    AdjMatrix(){
	    }
}
